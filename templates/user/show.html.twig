
{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ user.name}}</td>
        </tr>
        <tr>
            <th>Last Name</th>
            <td>{{ user.lastname }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_index') }}">Back To Main Page</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Edit</a>

{#    {{ include('user/_delete_form.html.twig') }}#}
{% endblock %}